# PaceAlgo — Consolidated Knowledge + Roadmap (2026-06-01)

Single-page synthesis after Phase 3–5: what we know, the (reality-adjusted) end goal,
and the path to get there. Supersedes scattered next-steps.

---

## 1. What we know for certain (validated this session)

**The edge:**
- Edge = **ML SELECTION within a gated population**, and it is **thin**.
- The raw deterministic signal population LOSES: structural setups / state-gated trades net
  **PF 0.6–0.8, WR 0.35–0.40, −0.2R/trade**, every pair, every year, even with NY gate.
- ML picking the top ~3% flips that to **PF 1.3–1.6** — the ML selection IS the entire edge.

**Where the edge lives (and where it doesn't):**
- Validated good only on **FX majors + NY session + ECN spread**.
- **FX is session-driven**: remove the NY gate and FX PF drops 1.27→1.11.
- **Does not generalize**: cross-asset (agnostic gate) PF — FX 1.11, crypto 1.05, metals
  0.95, indices 0.88 (mean 1.00, 0/4 classes ≥1.3). No universal edge exists.

**Best validated FX config (the one genuinely good, robust thing):**
- long (5 pairs) + USDCHF-short + META re-rank + POOLED top10 + proba sizing, 50 trees,
  ECN 0.5pip → **net PF 1.30 (1.52 sized), WR 0.51, ~9 trades/day, all years positive
  (2026 1.40–1.66), Pine budget 88%.**

**Levers that work / are dead (don't re-try the dead ones):**
- WORK: short side (density), pooled selection (lookahead-free ≈ hindsight), meta-labeling
  (+WR+PF), proba sizing (+PF, +variance), tree reduction to 50 (fits Pine, edge holds).
- DEAD: cutoff loosening, trend-align gate, dynamic exits (BE/trail), seed ensemble,
  sample-uniqueness weighting, short-specific features, TP/SL sweep (R=1.5 optimal),
  deterministic setups as a standalone edge, the universal single-model.

**Deployment reality:** Pine budget 5000 ops/bar. FX long+short = 88%, +META@50t = 88%.
Per-symbol indicator (no cross-symbol ranking needed). request.security 14/40.

---

## 2. The end goal — reality-adjusted (Nico-approved 2026-06-01)

Original aspiration: one indicator, **good results on any asset & any timeframe**, user-set
RR/ADR, backtest. **The data killed the "good results everywhere via one edge" part.**

**Accepted reframe — separate TOOL from EDGE:**
> **PaceAlgo = a universal, adaptive, self-calibrating signal + risk-management TOOL**
> (any asset/TF by construction, user-configurable RR/ADR, reproducible non-repaint
> backtest) **+ an optional ML "Confidence" overlay that delivers a real edge only where
> validated (FX first), expanding class-by-class via the ANN-009 router.**

- The **tool** is the universal, honest, sellable utility (no auto-edge promise).
- The **edge** is the premium AI overlay, FX-only today.
- "Good PF/WR" is a property of the **overlay**, not the tool. Market accordingly.

Honest truth: "good auto-results on every asset" is NOT achievable. "A solid universal
tool + a strong FX AI module + honest class-by-class AI expansion" IS.

---

## 3. How we get there — phased plan

**Phase 0 — Lock the proven asset (the spine). [highest value, lowest risk]**
Kernel-lock the FX config (primary long/short + meta, 50t) → snapshot thresholds/sizing/
gate → Pine export (4 cascades + feature engine + adaptive gate + R exec) → bit-exact +
ops<5000 verify. Outcome: one real, good, shippable thing (FX AI module).

**Phase 1 — Build the adaptive tool frame around it.**
Deterministic universal core in Pine: vol/trend context + **adaptive activity-window
detection** (replaces hardcoded NY) + user-selectable signal modes + **user RR/ADR risk**
+ **backtest panel** (PF/WR/MDD per chart/TF, non-repaint). FX AI module plugs in as the
"Confidence" overlay; off (or generic) on unsupported markets. Honest UX: tool everywhere,
AI badge where validated.

**Phase 2 — Industrialize per asset class (ANN-009 router).**
For each class, test with CLASS-APPROPRIATE structure (the untested fair test):
- **Indices first** — they trade a strong US cash/RTH session (analogous to FX-NY); the
  agnostic test that hurt them most (0.88) removed exactly that. RTH-gated indices are the
  most promising next AI module.
- Then crypto (own time/weekend effects), metals.
Add an AI Confidence overlay only for classes that clear the per-class ML bar (≥~1.3 net,
all-years-positive, walk-forward). Classes that don't clear it stay tool-only.

**Phase 3 — V1.5 / V2 backend (per original evolution).**
Monthly retrain → auto-ship new Pine; later cloud inference + webhook + dashboard.

**Revised launch gates (replace old §1.4 universal-edge premise):**
- (a) Tool bar: non-repaint verified, backtest reproducible bit-exact, clean UX, works on
  any chart without errors.
- (b) AI-overlay bar (per class): net PF target, all-years-positive, walk-forward robust,
  Pine bit-exact. Launch FX overlay when it clears; add classes as they clear.

**Immediate next action:** Phase 0 — lock + export the FX module. It de-risks everything
(gives a concrete, good product core) and is the substrate the tool frame plugs into.

---

## 4. UPDATE 2026-06-02 — Build-Order RESOLVED + neue Module (überschreibt §3-Reihenfolge)

**Nico-Entscheidung (Web-Session): RULE-CORE FIRST.** Phase-1-Tool-Frame kommt VOR dem
FX-Pine-Export. `deploy_pine/pace_algo_v1.pine` = selektiver Trend-Core (ADX-Gate + HTF +
Pullback-Entry, fixes TP/SL, 3 Profile, MTF-Dashboard, Backtest-Panel, regime-basiertes
Recommended-Panel — Variante A, KEIN In-Sample-Optimizer). ML-Export-Kette geparkt → V1.5.

**Forschungsstand (phase10-13, siehe `docs/module_registry.md`):**
- ✅ FX-NY (ML) gelockt: PF 1.51 sized · ✅ INDEX-DIPBUY gelockt: Holdout 15-21 PF 1.63
- 🟢 METAL-TREND_L Kandidat (27/27 Grid) · ❌ Crypto direktional FINAL ZU (echte Fees)
- Shorts: nur FX/USDCHF trägt; Index-/Metal-Shorts strukturell tot → Tool zeigt dort
  Shorts nur als Tool-Signal, nie als Edge-Versprechen.

## 5. V1-UX-Featureplan (Web-Session-Nachträge 2026-06-02 — Erkenntnisse GELOCKT)

**Gelockte Erkenntnisse (TP/SL-Philosophie):**
- Box-LÄNGE = Funktion von TP/SL-Abstand relativ zur Volatilität, NICHT vom Hebel.
  ATR-Normalisierung skaliert Boxen automatisch mit dem TF — Trading-Art steckt
  implizit im gewählten Timeframe.
- **HEBEL ≠ TP/SL-Platzierung.** Stop gehört an die strukturelle Invalidierung
  (Markt/Vola), Hebel ist reine Positionsgrößen-Entscheidung. Indikator kennt den
  Hebel nicht und koppelt TP/SL NIE daran (Swift Algo auch nicht).
- Zwei orthogonale Achsen sauber trennen: **Profile = Signal-SELEKTIVITÄT** (Strategy-
  Gruppe) vs. **Trading Style = Trade-HORIZONT/Box-Größe** (Risk-Gruppe) — UI-Labels
  entsprechend.
- **Frequenz/Qualitäts-Tradeoff als Designprinzip:** mehr Signale = verrauschter.
  Frequenz NIE als fixe Zahl hardcoden (8/Tag ≠ Ziel), sondern als User-Achse
  exponieren; ehrliches Tooltip-Framing. Net-PF vs. Frequenz beim Validieren messen
  (Phase-3-Logik auf den Regel-Core übertragen).

**Priorisierte Feature-Liste (NACH Core-Validierung auf Trend-Charts):**
1. **HOCH — Sensitivity-/Frequenz-Regler:** EIN Knopf skaliert ADX-Schwelle +
   Pullback-Strenge + adaptiven Multiplier zusammen. Empfehlungs-Panel schlägt pro
   Chart-TF eine Sensitivity vor (Swift-Tabelle-analog, aber regime-basiert).
2. **MITTEL — Multi-TP (TP1/TP2) + Teilausstieg + Break-Even-Stop:** TP1 ~1R sichern,
   SL auf Einstand, Rest läuft auf TP2=RR. Exit-GEOMETRIE (kein Fit) — hebt die
   angezeigte WR strukturell über 50–60% und ist der Haupt-Hebel für Nicos
   WR-Anforderung. Konkurrenz (SMRT) hat es.
3. **MITTEL — Trading-Style-Preset Scalp/Intraday/Swing:** dünne Komfort-Schicht
   (Struktur-Lookback + ATR-Band + RR-Default bündeln). *Status: Kern bereits in
   iter2 umgesetzt (Nico-Order 2026-06-02); RR-Default-Kopplung + Risk-Gruppen-Label
   noch offen.*
4. NIEDRIG — Trend-Stärke-Visualisierung (Zonen-Deckkraft nach Trendstärke).
5. NIEDRIG — MTF-Dashboard 4→8 TFs (4 reicht für V1).

**GTM-Vormerkung (ANN-007):** Onboarding-Bibliothek (Guides, Setup, Sensitivity-pro-
TF-Tabelle), dokumentierte Methodik als Education-Asset (Swift: „Conflux Strategy",
3.049 Member, $67/Monat — deren Wert ist groß teils Onboarding+Community).

**BEWUSST NICHT kopieren:** In-Sample-WR-Optimizer (Swift), „AI Sensitivity Optimizer"
(Flux) — Curve-Fit, verletzt Anti-Curve-Fitting-Lock.

## 6. PF-Verbesserungs-Leiter (CEO-Richtungsvorgabe 2026-06-02 — GELOCKT)

PF ist der Maßstab, WR ist Optik. Die ehrliche Leiter, in dieser Reihenfolge:

| Stufe | Hebel | Charakter |
|---|---|---|
| **1 (jetzt)** | **Exit-Geometrie:** Multi-TP (TP1 fix @1R — nie enger, §15-Lock) + Break-Even + Runner-Trail | Universell, ehrlich, verbessert die Payoff-VERTEILUNG. Erzeugt KEINEN Edge und wird nicht so vermarktet |
| 2 | **Selektion verschärfen:** MTF-Konfluenz vom Display zum harten Filter, ADX straffen | Weniger, bessere Trades |
| 3 | **Klassen-Spezialisierung (Router ANN-009):** pro Klasse ein walk-forward-validierter Parametersatz/Modul (siehe module_registry) | Validiert pro KLASSE, NIE per-Asset gefittet |
| 4 | **ML-Confidence-Overlay (V1.5):** 9-Feature-Ranker als Wahrscheinlichkeits-Filter auf Regel-Signale, nur wo Edge nachgewiesen (FX-Majors/NY) | ML rankt Signale, generiert keine |

**Die Verbesserungs-Maschine (Disziplin, jede Stufe):** EINE prinzipielle Hypothese pro
Iteration → out-of-sample über ≥4 diverse Assets → nur behalten wenn PF BREIT steigt
(nicht auf einem Asset) → dokumentieren. Kein Per-Asset-Tuning. (Musterbeispiel: das
echte HTF-Gate.)

**Ehrlichkeits-Boden:** Bleibt ein Asset trotz aller Stufen bei PF ~1.0 → WAIT-Panel,
dort nicht traden, keinen Edge faken. Ziel = „≥50% WR / PF >1.2 überall wo wir FEUERN",
nicht „auf jedem Asset erzwingen".

## 7. EDGE-AUSBAU-ROADMAP (Web-Session 2026-06-02 — GELOCKT)

**LEITPRINZIP (verbindlich): Die Validierungs-Maschine IST der cross-asset Edge-Mechanismus.**
Eine Verbesserung, die nur EIN Asset hebt = Curve-Fit. Eine, die über ≥4 diverse Assets OOS
den PF hebt = echter Edge (wie das HTF-Gate: „Trends richten sich aus" ist marktübergreifend).
Alles unten läuft durch den Gauntlet: 1 Hypothese → ≥4 Assets OOS → behalten nur wenn PF BREIT
hält/steigt UND Trade-Count sinnvoll → sonst verwerfen/WAIT. PF vor WR. Kein Edge → WAIT.

### Teil 1 — Universelle Verstärker (regime-agnostisch, verstärken den vorhandenen Trend-Edge)
- **H-EXIT** *(Stufe 1 — GETESTET 2026-06-03, reine Form ABGELEHNT)*: Trailing-Runner statt
  TP2-Cap. Gebaut als `runnerMode`-Input (Capped @ RR / Extended 3R / Free trail). Sweep über
  6 Märkte (Index/Metall/FX/Crypto): **Free trail ist NICHT robust** — gewinnt groß auf glattem
  Trend (US500 D 1.38→3.13) aber zerstört NAS100 4h (1.05→0.55), kostet BTC/EURUSD → verletzt
  Promotion-Gate. Extended 3R gemischt (US500 +.52, sonst flat/neg). **Capped bleibt Default.**
  ERKENNTNIS: TP2-Cap war nicht der Engpass — der weite SuperTrend-Trail (≈2.5·ATR) schützt
  Runner-Gewinne schlecht. Exit-Geometrie ist ein **regime-/TF-abhängiger** Hebel (→ ANN-024),
  kein universeller. Voller Scorecard: `results/benchmark_runs/2026-06-03_H-EXIT_sweep_verdict.md`.
  **NICO-CALL 2026-06-03:** Runner-Modi werden NICHT als Live-Perf-Toggle ausgeliefert —
  Capped ist einziger Live-Default. Free/Extended-Logik bleibt im Code, **gated hinter dem
  Regime-Router** (ANN-024), kein nackter User-Schalter (Footgun-Argument: Retail liest keine
  Docs, flippt „größere Gewinne", wird auf NAS100 zerlegt, churnt = exakt die Brand-Anti-These).
  Umgesetzt: `runnerMode` von `input.string` → interne Konstante `"Capped @ RR"`. *On-brand-Backlog:*
  Free-Trail als reine Backtest-Panel-VERGLEICHSZEILE („ein freier Trail hätte hier PF auf 0.55
  gesenkt") = Transparenz-Feature, NIE als Live-Modus.
  Folge: **H-EXIT-v2** (engerer Chandelier/ATR-Runner-Trail) → Backlog NACH dem Router (dann
  pro Regime getunt statt gegen einen Regime-Durchschnitt = die Version die gewinnt).
- **H-REGIME** *(NÄCHSTE Iteration — größter Hebel, Nico-locked Scope 2026-06-03)*: bessere
  Regime-Trennung als das grobe ADX-Gate. **KEIN Kandidaten-Buffet (ER/Hurst/R² gegeneinander
  = Random-Search, §5-verboten).** EINE Hypothese mit Prior: **Kaufman Efficiency Ratio (ER)**.
  Begründung: (i) billigste bit-exakte Pine-Implementierung (kritisch wegen Python↔Pine), (ii)
  bounded [0,1], stabil auf unseren Bar-Counts, (iii) semantisch das Trend-Effizienz-Maß, das den
  Runner gated. *Hurst = Rabbit-Hole (instabil auf kurzen Fenstern, teuer). Regr-R² = sauberer
  Fallback NUR falls ER scheitert.* **Zahlt doppelt:** (a) schaltet das Runner-Routing (ER hoch →
  Extended/Free, ER niedrig → Capped) und macht Free-Trail vom Footgun zum validierten Gewinn,
  UND (b) verschärft die Entry-Selektion (Stufe-2-Zweck). Protokoll: ER allein, ≥4 Assets/Klasse
  OOS, Promotion nur wenn Klassen-Median-PF steigt UND keine Klasse regrediert, sonst WAIT, kein
  Schwellen-Senken.
  **ERGEBNIS 2026-06-03 — ER (len10/thr0.30) BESTEHT DAS GATE NICHT.** Sweep 6 Märkte × 4 Modi:
  Runner-routing hilft GOLD +0.35 / US500 +0.10, regrediert aber NAS100 4h −0.07 (in-scope) + BTC;
  Entry-gate hebt nur NAS100 4h, kollabiert Daily-Frequenz (US500 18→4, GOLD 12→2) und regrediert
  FX hart (EURUSD 1.21→0.95, GBPUSD 0.85→0.33); Both überall dominiert. Kein Schwellen-Tuning
  (verboten). ER-Wiring zurückgebaut (Live = Baseline), ER-Impl bleibt in Commit `d0b13c7` für den
  ANN-024-Router. Scorecard: `results/benchmark_runs/2026-06-03_H-REGIME-ER_sweep_verdict.md`.
- **H-CONFLUENCE** *(Hypothese)*: orthogonale Bestätiger statt tieferem Filter-Stack (hebt auch
  Trade-Count). Intern: HTF + Momentum + Breakout-Struktur unabhängig. Cross-sektional
  (generalisiert): Asset nur handeln wenn Korrelat bestätigt (Gold↔DXY, Aktie↔Index, Alt↔BTC).

> **STRATEGISCHE WENDE (2026-06-03, ENTSCHIEDEN, nach 2 Negativ-Ergebnissen H-EXIT + H-REGIME-ER):**
> Präzise Lehre: nicht „Teil 1 ist tot" sondern **„global-uniform trägt nicht".** Die Struktur trägt
> Signal (ER-Runner-routing: Trend-Daily +, Chop/Hochvol −) — eine globale Schwelle über alle
> Klassen/TFs zerstört es. ER ist damit der **erste gemessene Router-Input** (pro Klasse/TF
> kalibriert), nicht tot. **Entscheidung: nächster Zyklus = ANN-024-Router, inkrementell. KEIN
> separater globaler H-CONFLUENCE-Lauf** — Confluence-Paare sind klassen-gebunden (DXY→FX/Metalle,
> Alt↔BTC→Crypto); global angewandt = Negativ #3. Confluence wird klassen-skaliert IM Router getestet.
> Produkt parallel: Tier-A sitzt an seiner ehrlichen PF-Decke → verkaufbar polieren, Router als
> eigener Coverage-Track. Voller Contract: ANN-024 §Router-Contract v1.

> **UPDATE 2026-06-03 — Router-Input #1 (ER-Runner-routing) GETESTET, BESTEHT NICHT.** Scoped auf
> Index+Metall-Daily, prinzipielle Perzentil-Schwelle q0.50: **beide Klassen-Mediane regredieren**
> (Index 1.38→1.29, Metall 1.82→1.74). Einzige Plus: GOLD +0.35 (konsistent) und NAS100 +4.0 (11t/
> 1loss = Rauschen); SILVER −0.50, US500 −0.09 (US500 war bei abs. thr0.30 noch +0.10 → nicht
> robust). **Meta-Grenze:** Exit-Routing ist mit unseren Daten NICHT validierbar — Daily 9–18 Trades
> (Rauschen), Intraday-Routing schadet. **3. Negativ auf dem Exit/Regime-Knopf-Pfad → der Pfad ist
> geschlossen.** ER raus (per Contract). Verdikt: `results/.../2026-06-03_ANN024-router-ER_input1_verdict.md`.
> **FORK an Nico:** statt Confluence blind nachzuschieben → (1) MR-Modul (Coverage, echter Hebel)
> ODER (2) Produkt-Polish (Tier-A an der Decke = ship-fähig). Empfehlung: weg vom Knopf-Drehen.

### Teil 2 — Coverage-Sprung: zweite Routing-Dimension (Architektur, ANN-024)
- **H-MR + Regime-Router** *(größeres Vorhaben, eigener Zyklus)*: Mean-Reversion-Modul, NUR in
  Range-Regimes aktiv (Fade an Band-Extremen → Range-Mitte, Stop jenseits Band). Regime-Detektor
  aus H-REGIME = Router-Schalter. Coverage = Vereinigung regime-passender Edges, WAIT schrumpft
  ehrlich. Modul nur klassen-/regime-weit validiert, nie per-Asset (ANN-024).

### Teil 3 — Spätere Stufen
- **Portfolio-Sicht**: viele dünne, UNKORRELIERTE Edges aggregieren zu glatterer Gesamt-Equity
  (selbst PF~1.1-Assets wertvoll wenn unkorreliert). Mögliche Multi-Asset-Ansicht im Produkt.
- **ML-Overlay (V1.5+, letzte Sprosse)**: normalisierte-Feature-Ranker als P(Win)-Filter auf
  Regel-Signale, walk-forward pro Klasse. Rankt Signale, generiert keine. Erst wenn Regel-Hebel
  erschöpft.

### Benchmark-Queue (EINE nach der anderen, PF breit = Schiedsrichter)
1. ~~**H-EXIT** (Trailing-Runner)~~ ✅ GETESTET 2026-06-03 — reine Free-Trail-Form abgelehnt
   (nicht robust); Capped bleibt Default; Exit-Geometrie → regime-abhängig (ANN-024).
2. ~~**H-REGIME** (ER)~~ ✅ GETESTET 2026-06-03 — ER besteht das Gate nicht (regime/TF-heterogen);
   ER-Impl in `d0b13c7` aufgehoben für den ANN-024-Router.
3. **ANN-024-Router (Teil 2) — ENTSCHIEDEN als nächster Zyklus, inkrementell** (ein validierter
   Input nach dem anderen). Input #1 = **ER-Runner-routing klassen-skaliert** (Index-Daily +
   Metall-Daily, per-Klasse-Schwelle aus fixem ER-Quantil, OOS ≥4/Klasse). Input #2 = **Confluence
   klassen-gepaart** (DXY→FX/Metalle, Alt↔BTC→Crypto). Dann H-MR-Modul für Range-Regimes.
4. ~~globaler H-CONFLUENCE-Lauf~~ — VERWORFEN (klassen-gebunden → gehört in den Router, nicht global).
- *(Backlog)* **H-EXIT-v2**: engerer Chandelier/ATR-Runner-Trail nach TP1, pro Regime getunt (nach Router).

**Exit-Modell-Integrität (CEO-locked):** TP1 ≥ 1R (Sub-1R-Token-TP = WR-Inflation,
verworfen) · Win-Definition = Netto-R > 0 (TP1+BE = echter Win) · Panel zeigt Outcomes
GESPLITTET (TP2/Trail/BE/Loss), PF/Expectancy als Headline, WR nachgeordnet.

**Reihenfolge ab jetzt:**
1. **V1 Regel-Core validieren** (TV-MCP, Heim-PC): trendendes + rangebound Asset;
   Ziel WR ~45-55%, PF >1.2 auf Trends, im Chop still. Feintuning mit Nico
   (ADX-Schwelle/Pullback-Tiefe/RR-Default) — global, NIE per-Asset-Fit.
2. **V1.5a Klassen-Router + deterministische Module einstecken:** INDEX-DIPBUY (Daily)
   und ggf. METAL-TREND_L als zuschaltbare validierte Setups/Badges — kein ML-Budget nötig.
3. **V1.5b ML-Confidence-Overlay FX:** 9-Feature-Ranker reaktivieren (Export-Kette
   entparken), nur FX-Majors, als Premium-Badge auf dem Regel-Signal, bit-exact.
4. **Distribution-Pipeline (ANN-007)** parallel vorbereitbar.
5. Forschung weiter (geringere Prio): Crypto cross-sektional; Index-Universum-Pflege.

## 8. AKTUELLE PRIORITÄTEN (2026-06-08 — Web-Session-Order, GELOCKT, überschreibt die Queue oben)

**Gelockte Haltung (Kern-Entscheidung):** Der Tier-A-Trend-Core ist das ehrliche TOOL und
sitzt an seiner ehrlichen PF-Decke (H-EXIT + H-REGIME + MR alle sauber getestet + abgelehnt).
**Der Core wird NICHT wieder aufgemacht.** „Besser werden" = **validierte Tier-B-Module shippen
+ Coverage Modul für Modul wachsen lassen**, NICHT den Core zum Performen zwingen. Der Edge sitzt
in Tier B und ist stark (FX-NY-ML net PF 1.51, INDEX-DIPBUY Holdout 1.63, METAL-TREND_L 🟢) — er
wird sichtbar, sobald die Module ins Pine-Produkt kommen.

### Priorität 1 (aktiv) — FX-NY-ML-Overlay V1.5: bit-exact Pine-Export
Stand: Cascade-Math bit-exact (0.0) ✅, Codegen kompiliert in TV 3.2.0 ✅, **8/9 Features bit-exact**.
- **Block 1 — 2 offene bit-exact-Risiken:**
  - (a) `htf_4h_atr_percentile_100` ✅ **GESCHLOSSEN 2026-06-08** — Pine `_pf_pctrank100` an die
    Trainings-Definition (`rolling(100,min_periods=100).rank(pct=True)`, method='average', Fenster
    inkl. self) angeglichen; algebraische Parität lokal bewiesen (0.0 diff, inkl. Ties/Warmup,
    `scripts/verify_pctrank_pine_parity.py`). Gilt für Chart-TF + 1h + 4h.
  - (b) `htf_4h_rsi_14` 4h-Alignment 🟡 **OFFEN** — Hypothese: Pine `request.security(...,expr[1],
    lookahead_off)` = Doppel-Shift (einen 4h-Bar zu alt vs. pandas `shift(1)`+ffill auf nativen
    4h-Bars). Braucht **numerische TV-Verifikation auf identischen Bars** (Look-Ahead vs. Staleness,
    beide Richtungen teuer → NICHT per Reasoning ändern). Protokoll in HANDOFF §19.
- **Block 2** — Short- + Meta-Cascades + Selektions-Kette (gen→meta→POOLED→Sizing) ins Skelett,
  dann Pine-Live-Verifikation der finalen SIGNALE gegen das Python-Modell (nicht nur Features).
- **Erst wenn V1.5 whole-chain bit-exact + OOS-revalidiert ist** → als FX-Modul shippen,
  Coverage-Matrix FX-NY-ML → „Edge-Validated (live)".

**🚦 EINTRITTSKRITERIEN für „Edge-Validated (live)" (Nico-locked 2026-06-08 — gating, NICHT für den Block-2-Build):**
Block 2 darf normal starten. Aber das FX-Modul wird ERST auf „Edge-Validated (live)" gesetzt, wenn ZUSÄTZLICH zum whole-chain bit-exact:
1. **Non-Repaint-Replay auf das neue `lookahead_on + [1]`-Idiom.** Alignment ist verifiziert (welcher Bar), aber `lookahead_on` ist das Flag MIT Lookahead-Risiko — die Alignment-Prüfung beweist NICHT, ob es beim Vorwärts-Steppen repaintet. Bar-Replay-Test (Steps vorwärts, historische Werte byte-identisch) auf der neuen Konstruktion nachholen. Das `[1]` sollte es absichern, aber empirisch beweisen (wie der HTF-Gate-Replay 2026-06-02).
2. **Exakter RSI-Pin (1e-5)** im Block-2 Whole-Chain-Check mit VOLLER Historie (das live warmup-limitierte Stückwerk ersetzen). Gilt für alle ta.*-Features (rsi/atr/percentrank/ema), nicht nur 4h-rsi.
PF vor WR, Disziplin bleibt.

### Priorität 2 (nach V1.5-Ship) — Module fahren die sichtbaren Signale wo aktiv
Auf abgedeckten Assets (FX zuerst, dann INDEX/METAL) zeigt das Produkt die MODUL-Signale (Edge);
nur auf nicht-abgedeckten Assets fällt es auf den ehrlichen Tool-/WAIT-Core zurück. WAIT-Zonen
schrumpfen **legitim** (durch mehr validierte Module), nicht durch Tuning. Routing über die
Coverage-Matrix (Asset/Klasse/Regime → zuständiges Modul oder WAIT).

### Priorität 3 (laufend) — Modul-Discovery-Maschine = die eigentliche Edge-Maschine
- INDEX-DIPBUY + METAL-TREND_L ebenfalls nach Pine exportieren/shippen (wie FX).
- Denselben validierten Research-Prozess auf NEUE Klassen/Regimes/Setups ansetzen (Crypto-
  spezifisch, Vola-Breakout, weitere FX-Sessions …). Jedes = eigenes Mini-Projekt, durch die
  Benchmark validiert oder verworfen. Coverage wächst Modul für Modul.
- Disziplin pro Modul: walk-forward/OOS, PF breit über die Klasse, nie per-Einzel-Asset gefittet.
  Was nicht validiert → bleibt WAIT, kein Forcieren.

### Optional — letzter ehrlicher Core-Hebel (nur falls noch NICHT gelaufen)
Cross-sektionale Konfluenz (Gold↔DXY, Aktie↔Index, Alt↔BTC). **Status:** der globale H-CONFLUENCE-
Lauf wurde 2026-06-03 bewusst VERWORFEN (klassen-gebunden → gehört in den ANN-024-Router, nicht
global). Falls als klassen-gepaarter Test gewünscht: EINE Hypothese, ≥4 Assets OOS, PF breit,
NIEDRIGE Erwartung, ein Test → Ergebnis akzeptieren, KEINE neue Core-Tweak-Schleife.

### Kommerziell/UX (parallel, ANN-007)
Coverage-Matrix wird zur „Supported Markets"-Anleitung im Onboarding (Nutzer dahin führen wo
validierte Edge ist). Marketing leads mit „validierter Edge auf FX/Indizes/Metallen", nicht
„funktioniert überall". Schmal-aber-echt > breit-aber-fake.

### Guardrails (gelockt)
- Core NICHT wieder aufmachen (an der ehrlichen Decke). Working-Script geschützt.
- PF vor WR. Eine Hypothese, ≥4 Assets OOS, nur behalten wenn PF breit hält/steigt.
- Wo kein Edge: WAIT. Keine 100%-Coverage erzwingen, kein Curve-Fit.
